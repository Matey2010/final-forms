name: final_forms
description: A standalone Flutter form package with validation, custom field builders, and programmatic control. Zero external dependencies.
version: 1.0.0
homepage: https://github.com/user/final_forms
repository: https://github.com/user/final_forms
issue_tracker: https://github.com/user/final_forms/issues
topics:
  - form
  - validation
  - widget
  - ui

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^5.0.0
